[build]
  base    = ""
  publish = "/build"
  command = "yarn build --type=UMD_DEV && cd fixtures/dom/ && yarn && yarn prestart && yarn build"


[[redirect]]
from = "/*"
to = "/index.html"
status = 200